{
  "name": "auto-web",
  "version": "1.0.0",
  "description": "Auto web project",
  "scripts": {
    "dev": "live-server --port=3000 --host=0.0.0.0",
    "start": "live-server --port=3000 --host=0.0.0.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
